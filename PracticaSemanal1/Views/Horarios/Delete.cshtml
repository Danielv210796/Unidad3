@model PracticaSemanal1.Models.Horarios

@{
    ViewBag.Title = "Delete";
}

<h2 class="text-danger">Delete</h2>

<h3 class="text-danger">Are you sure you want to delete this?</h3>
<div>
    <h4 class="text-danger">Horarios</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt class="text-danger">
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>

        <dd class="text-danger">
            @Html.DisplayFor(model => model.Nombre)
        </dd>

        <dt class="text-danger">
            @Html.DisplayNameFor(model => model.Hora)
        </dt>

        <dd class="text-danger">
            @Html.DisplayFor(model => model.Hora)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions  text-danger">
            <input type="submit" value="Delete" class="btn-primary" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
